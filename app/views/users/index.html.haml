- provide :title, 'Users'
- provide :body_class, 'user-list'

%h1 Users

%ul.users
  - @users.each do |user|
    %li.user
      = link_to user.name do
        = gravatar_for user, size: 60
        %span.name= user.name
